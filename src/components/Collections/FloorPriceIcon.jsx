import Icon from '@ant-design/icons';

import { ReactComponent as FloorPrice } from './FloorPrice2.svg';

const FloorPriceIcon = (props) => {
  return <Icon component={FloorPrice} {...props} />;
};

export default FloorPriceIcon;
